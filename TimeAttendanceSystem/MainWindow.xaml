<Window x:Class="TimeAttendanceSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow"  WindowStartupLocation="CenterScreen">
    <Window.Resources>

        <!-- The WPF DataGrid (WPF Toolkit version) has a bug that throws a format exception
        when a WPF DataGrid is bound to a SelectedItem property, as we do here. The Ignore New
        Item Plave Holder Converter works around this bug. For more information on the bug and
        the converter, see Nigel Spencer's Blog (2009-04-30), which discusses the bug and provides 
        the fix. Link to blog post can be found in the remarks to the value converter.  -->


        <!-- DataGrid Background -->
        <LinearGradientBrush x:Key="BlueLightGradientBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Offset="0" Color="#FFEAF3FF"/>
            <GradientStop Offset="0.654" Color="#FFC0DEFF"/>
            <GradientStop Offset="1" Color="#FFC0D9FB"/>
        </LinearGradientBrush>

        <!-- DatGrid style -->
        <Style TargetType="{x:Type Grid}">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Background" Value="{StaticResource BlueLightGradientBrush}" />

        </Style>
    </Window.Resources>
    <DockPanel  LastChildFill="True">
    <Grid Margin="0,0,0,0">
        <Menu DockPanel.Dock="Top" Height="28" FontSize="14" VerticalAlignment="Top">
            <Menu.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FFEFF2F3" Offset="0"/>
                    <GradientStop Color="White" Offset="1"/>
                </LinearGradientBrush>
            </Menu.Background>
            <MenuItem Header="Company" >
                <MenuItem x:Name="MenuItemEmp" Header="Employee" Width="200" Click="MenuItemEmp_Click"/>
                <MenuItem x:Name="MenuItemShift" Header="Shift"  Width="200" Click="MenuItemShift_Click"/>
                <MenuItem x:Name="MenuItemCat" Header="Category"  Width="200" Click="MenuItemCat_Click"/>
                <MenuItem x:Name="MenuItemEmpType" Header="Employee Type"  Width="200" Click="MenuItemEmpType_Click"/>
                <MenuItem x:Name="MenuItemSec" Header="Sections"  Width="200" Click="MenuItemSec_Click"/>
                <MenuItem Header="Departments"  Width="200" Click="MenuItemDept_Click"/>
                <MenuItem x:Name="MenuItemDiv" Header="Division"  Width="200" Click="MenuItemDiv_Click"/>
                <MenuItem x:Name="MenuItemLoc" Header="Location"  Width="200" Click="MenuItemLoc_Click"/>
                <MenuItem x:Name="MenuItemCity" Header="City"  Width="200" Click="MenuItemCity_Click"/>
                <MenuItem x:Name="MenuItemHol" Header="Holiday" Width="200" Click="MenuItemHol_Click"/>
                <MenuItem x:Name="MenuItemCrew" Header="Crew"  Width="200" Click="MenuItemCrew_Click"/>
            </MenuItem>
            <MenuItem Header="Leave">
                <MenuItem x:Name="MenuItemLvApp" Header="Application" Width="200" Click="MenuItemLvApp_Click"/>
                <MenuItem x:Name="MenuItemSLv" Header="Short Leave"  Width="200" Click="MenuItemSLv_Click"/>
                <MenuItem x:Name="MenuItemQuota" Header="Quota"  Width="200" Click="MenuItemQuota_Click"/>
            </MenuItem>
            <MenuItem Header="Device">
                <MenuItem x:Name="MenuItemRdr" Header="Readers"  Width="145" Click="MenuItemRdr_Click"/>
            </MenuItem>
        </Menu>
        
    </Grid>
    </DockPanel>
</Window>
